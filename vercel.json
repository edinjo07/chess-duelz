{
  "version": 2,
  "builds": [
    {
      "src": "backend/public/**",
      "use": "@vercel/static"
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/backend/public/$1" }
  ]
}
